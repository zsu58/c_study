#include <stdio.h>

void main()
{
  
}

/*
char *gets(char *buffer);
buffer : 입력받은 문자열을 저장할 메모리의 주소
정상적이면 인자로 전달받은 메모리의 주소, 오류가 발생하면 NULL을 반환
표준 입력 장치(stdin)에서 문자열을 입력받는 함수
보안에 취약해 gets_s()를 사용하는 것이 더 좋음

int puts(const char *string);
string : 출력할 문자열이 저장된 메모리의 주소
정상적이면 음수가 아닌 값을 반환, 오류가 발생하면 EOF를 반환
표준 출력 장치(stdout)에 문자열을 출력하는 함수
cf. 문자열이 출력된 이후 자동으로 개행 문자를 출력
*/


